# javascriptsubsequence

The code checks if a smaller array (_sub) is a
subsequence of the main array (_arrY). 
Main issue with this code is that it does not
check for the order, just for the values.
